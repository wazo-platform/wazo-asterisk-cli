extra_config_files: '/etc/wazo-asterisk-cli/conf.d'

# Connection info to the authentication server
auth:
  host: localhost
  port: 80
  https: false

amid:
  host: localhost
  port: 9491
  prefix: null
  https: false
